<article class="flex flex--items-center flex--justify-between push-lg--bottom">
  <%= link_to "#{user.name} #{at user}", user %>
  <%= link_to "Follow", [user, :follow], method: :post, class: "btn btn--secondary btn--small" unless user.followed? %>
</article>
